services:
  app:
    build: .
    image: saude-pilates:latest
    container_name: saude-pilates
    restart: unless-stopped
    ports:
      - "0.0.0.0:3000:3000"
    env_file:
      - ${ENV_FILE:-.env}
    # So the app can reach MySQL/DB on the host (use host.docker.internal in DATABASE_URL)
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.30.0.0/24
